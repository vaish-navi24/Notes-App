<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">

        <div class=" w-full flex justify-end gap-6">
            <a href="/profile" class="rounded-md px-3 py-2 text-sm inline-block text-white" >Go to Profile </a>
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block">Log-out</a> 
        </div>

        <br>

        <h5 class="mb-5">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea name="content" class="w-1/3 resize-none bg-transparent border-2 rounded-md border-zinc-800 p-3 mb-2 block" placeholder="whats on your mind ?">
                <%= post.content %>
            </textarea>
            <input type="submit" class=" w-40 px-3 py-2 text-sm bg-yellow-500 block rounded-md text-black" value="Save Changes">
    
        </form>
    </div>

</body>

</html>
